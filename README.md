# RNN-Text-Classification
This notebook presents an end-to-end deep learning workflow for text classification using recurrent neural networks.  Two models are implemented and compared: Simple RNN and LSTM(Long Short-Term Memory).  Model performance is evaluated using accuracy and a confusion matrix.
